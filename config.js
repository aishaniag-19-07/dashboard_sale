/**
 * config.js — Supabase configuration for the Sales Dashboard.
 *
 * In a plain HTML project the browser cannot read .env files directly.
 * These values come from the .env file and are safe to expose because
 * SUPABASE_ANON_KEY is a publishable key (Row Level Security enforces access).
 *
 * If you clone this repo, copy .env.example → .env and fill in your project values,
 * then update this file accordingly.
 */

const SUPABASE_URL = "https://lphusvuslgeezvpurlmj.supabase.co";
const SUPABASE_KEY = "sb_publishable_CgvgcJJQHsB0lG6w0sJauw_MgIva20y";
